#include<stdio.h>
# define MAX 30
int stack[MAX];
int top=-1;
int main() {
    int ch=1, option;
    while(ch) {
        printf("\n enter the choice");
        printf("\n 1.push  \n 2.pop  \n 3.peek");
        scanf("%d",&option);
        switch(option)
        {
            case1: printf("\n enter the data");
            scanf("%d",)
        }
    }

}